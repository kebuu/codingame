package tictactoe.player;

import minmax.Player;

import java.util.Optional;

class MinMaxPlayer implements TicTacToePlayer {

    public Optional<Player.Action> play(Player.GameState gameState) {
        return Optional.empty();
    }
}
